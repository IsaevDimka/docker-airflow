# Docker PostgreSQL Replication

Docker Airflow, run:
```shell
make init
chmod +x airflow.sh
```

## Requirements:

* Docker

## Commands:

```shell
./airflow.sh info
```

## UI
```shell
http://localhost:8080
```
